# Docker Instructions

- Use multi-stage builds with minimal runtime images.
- Run as non-root and define health checks.
- Keep secrets out of images and source.
- Use env-driven configuration only.
- Document compose usage and local dev instructions.
